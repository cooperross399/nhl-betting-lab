# Local-only credential file. Never commit this file with a real value.
# The production credential is the GitHub secret NHL_ODDS_API_KEY.
#
#   cp .env.example .env && chmod 600 .env
#
# Only the two names below are ever read from .env; everything else is ignored.
NHL_ODDS_API_KEY=
NHL_ODDS_API_BASE_URL=
